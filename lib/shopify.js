/**
 * Shopify Product Search - stub
 * Product search now happens CLIENT-SIDE in the widget
 * because Shopify blocks server-side requests from Vercel
 */
export async function searchProducts() {
  return [];
}
